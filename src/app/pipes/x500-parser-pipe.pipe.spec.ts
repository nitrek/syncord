import { X500ParserPipePipe } from './x500-parser-pipe.pipe';

describe('X500ParserPipePipe', () => {
  it('create an instance', () => {
    const pipe = new X500ParserPipePipe();
    expect(pipe).toBeTruthy();
  });
});
