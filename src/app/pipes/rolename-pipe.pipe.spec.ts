import { RolenamePipePipe } from './rolename-pipe.pipe';

describe('RolenamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RolenamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
